#set( $symbol_pound = '#' )
${symbol_pound} Usage

Add the following lines to use this project as the parent POM:

```xml
<parent>
    <groupId>$project.groupId</groupId>
    <artifactId>$project.artifactId</artifactId>
    <version>$project.version</version>
</parent>
```
